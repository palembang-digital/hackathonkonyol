---
import Emojifield from "~/components/emojifield.astro";

const widths = [100, 200];
const sizes = "(min-width: 640px) 42vw, 67vw";
---

<section class="relative h-full bg-black">
  <Emojifield />
  <!-- <div id="splash-bg-fallback" class="absolute inset-0 hidden opacity-40">
    <HeroImage />
  </div> -->
  <div class="relative grid h-full place-items-center">
    <h2 class="flex flex-col items-center gap-2 sm:gap-4 sm:self-auto">
      <!-- <Icon name="logomark" class="size-24 text-white md:size-32" /> -->
      <div
        class="gradient-text pb-4 text-center font-extrabold tracking-tighter text-8xl"
      >
        Hackathon
        <br />
        Konyol
      </div>
      <div
        class="gradient-text text-center font-bold tracking-tighter text-2xl"
      >
        🗓️ Sabtu, 20 April 2024
        <br />
        📍 PalComTech Basuki Rachmat
      </div>
    </h2>
    <!-- <div
      id="astronaut"
      class="w-2/3 max-w-3xl self-start sm:w-10/12 sm:justify-center sm:self-auto"
    >
      <Image
        class="h-1/2 w-1/2 object-cover"
        src={partyingFace}
        widths={widths}
        sizes={sizes}
        loading="eager"
        alt="A partying face"
      />
    </div> -->
  </div>
</section>

<noscript>
  <style>
    #splash-bg-fallback {
      display: block;
    }
  </style>
</noscript>

<style>
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }

    100% {
      transform: translate3d(0, 30px, 0);
    }
  }

  #astronaut {
    animation: float linear 2.5s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    #astronaut {
      @apply animate-none;
    }

    :global(#starfield) {
      @apply hidden;
    }

    #splash-bg-fallback {
      @apply block;
    }
  }
</style>
